Home Reading Library

Getting started

Requirements:
- .NET SDK 10
- Node + Angular CLI (Angular 21 recommended for the frontend)

Build and test:
- dotnet restore
- dotnet build
- dotnet test

Run the web host:
- dotnet run --project HomeReadingLibraryWeb

MongoDB scripts are available under the mongodb/ directory.